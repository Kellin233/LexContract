# =============================================================================
# LexTrace — 依赖清单（当前合同证据链链路实际需要）
# 安装方式：
#   pip install -r requirements.txt
# 说明：src/document/requirements.txt 与 src/retrieval/requirements.txt 是子模块的
#       细粒度版本；本文件是主流程（解析/入库/检索/证据链/评测）的合并清单。
# =============================================================================

# LLM 客户端：DeepSeek / MiMo / vLLM / OpenAI 兼容 API（src/models/）
openai>=1.0

# YAML 配置解析（src/core/runner.py）
pyyaml>=6.0

# .env 环境变量管理（src/utils/env_config.py 等）
python-dotenv>=1.0

# 数据模型（src/contract/*、src/retrieval/*、src/contract/eval/*）
pydantic>=2.7

# PostgreSQL + pgvector（文档入库 + 向量检索）
psycopg[binary]>=3.2
pgvector>=0.3.6

# 文档解析（src/document/parser.py）
docling>=2.4

# 本地多语 embedding / cross-encoder 重排（bge-m3 / bge-reranker）
sentence-transformers>=4.1.0

# BM25 查询分词（src/retrieval/tokenizer.py）
jieba>=0.42,<1

# 科学计算
numpy>=1.24

# ---------------------------------------------------------------------------
# 可选 / 遗留模块（旧 deep-research 流程，当前主链路不再使用）
# ---------------------------------------------------------------------------
# langsmith>=0.1.0        # LangSmith 追踪（src/utils/tracing.py，惰性导入，可选）
# aiohttp>=3.8            # 遗留 web 工具（src/tools/）
# matplotlib>=3.7         # 遗留评测图表（evaluation/）
# datasets>=2.14          # 遗留评测数据加载（evaluation/benchmarks/）
# pandas>=2.0             # 遗留评测数据处理（evaluation/）
